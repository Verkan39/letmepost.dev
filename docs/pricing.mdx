---
title: Pricing
description: Free during alpha. Flat-rate per org once billing turns on, with a real free tier and no per-profile tax.
icon: tag
---

<Note>
letmepost.dev is in alpha. The hosted SaaS is **free for everyone today** while we shake out platform-review timing. The tier shape below is locked for launch and what you'll pay when alpha ends. The canonical surface is [letmepost.dev/pricing](https://letmepost.dev/pricing).
</Note>

## Pricing principles

- **Flat rate, not per-profile.** The most-cited complaint about every incumbent is per-profile or per-seat pricing. We don't tax you for connecting more accounts.
- **A real free tier, not a trial.** Persistent, indefinite, sized so an indie dev or hobby project never has to upgrade.
- **Self-host stays free, forever.** The OSS image is the same code as the hosted SaaS. No feature gate, no open-core trick. [Self-host docs](/self-host/quick-start).
- **No contracts, no auto-renewal abuse.** Month-to-month, cancel anytime, downgrade anytime.

## Tiers

Pricing is per org, not per account. The number on the card is what your whole org pays.

| tier | price | what you get |
|---|---|---|
| **Free** | $0 / mo | 50 posts / month. All 7 platforms, all API surface. BYO platform credentials. MCP server (1 namespace). Community Discord support. |
| **Pro** | $79 / mo | 2,000 posts / month. Hosted OAuth (no BYO required). All webhook events. 30-day publish logs. Email support, 24h response. |
| **Business** | $299 / mo | 25,000 posts / month. White-label OAuth (your brand). 99.9% uptime SLA. 180-day publish logs. Priority Slack + email, 4h response. 5 MCP namespaces. |
| **Scale** *(coming soon)* | $799 / mo | 250,000 posts / month. Sub-org isolation for client work. Rate-limit budget uplift. Dedicated channel + monthly review. 365-day publish logs. Unlimited MCP namespaces. |
| **Self-host** | $0 / mo | Same code as hosted. BYO Postgres + Redis. BYO platform credentials. No license server, no telemetry. |
| **Enterprise** | Custom | Unlimited posts. SSO + SCIM. Custom SLA + DPA. Named CSM. Security + procurement review. |

Annual billing saves 2 months on every paid tier when it turns on.

Scale is marked Coming Soon because sub-org isolation, its headline feature, is still landing. Pricing is locked at $799/mo so you can plan around it.

## What's actually live

- **Bluesky** and **X** are live for every signup today.
- **Instagram**, **LinkedIn**, **Facebook Pages**, **Threads**, and **Pinterest** are gated on per-platform developer-portal reviews. Each ships as the review clears. Same API, same error envelope.
- Live status is queryable at `GET /v1/platform-versions` and tracked in the [changelog](/changelog).

## What's free during alpha

Everything. While we're in alpha, the hosted SaaS is free for everyone, there's no quota, no rate-limit ceiling beyond what's enforced for abuse prevention, and no card on file.

In return, you should expect:

- **No SLA today.** We're stable enough for production workflows but not promising one.
- **Migration help when billing turns on.** You'll know the prices, get advance notice, and have time to decide. Any workflow that connected during alpha gets a 60-day grace window after announcement before charges begin.
- **Approval timing is upstream.** Most of our platforms gate launch on their own developer-portal reviews. If your business depends on a specific platform that's still in review, self-host with your own credentials is the immediate path.

## What we don't charge for

- **Connected accounts.** Connect every platform, every account, every Page. No per-row tax.
- **Profiles.** A single org can carve into N profiles for per-client isolation. Profiles are free.
- **Team members.** No per-seat ladder.
- **Webhook endpoints.** Register as many as you need, we'll deliver to all of them.
- **API keys.** Mint a key per service, per script, per cron. Keys are free.
- **Webhook deliveries / retries.** Built into the contract, not a billable line item.

## Updates

The instant pricing changes, this page updates and the [changelog](/changelog) gets a note. Subscribe to that or watch [GitHub releases](https://github.com/rosekamallove/letmepost.dev/releases).
