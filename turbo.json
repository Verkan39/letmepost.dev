{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": [
        "NOTION_TOKEN",
        "NOTION_BLOG_DATABASE_ID",
        "PUBLIC_POSTHOG_KEY",
        "PUBLIC_POSTHOG_HOST"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "env": [
        "CI",
        "DATABASE_URL",
        "TEST_DATABASE_URL",
        "BETTER_AUTH_SECRET",
        "KEK_MASTER",
        "REDIS_URL"
      ]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {}
  }
}
